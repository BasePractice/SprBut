package ru.sprbut.m21.missing;

/**
 * Зависимость, которую забыли зарегистрировать в контексте.
 * <p>
 * Интерфейс существует, класс-реализация существует, но ни одна конфигурация
 * не объявляет бин — контейнер узнаёт об этом только в момент внедрения.
 */
public interface PaymentGateway {

    /**
     * Идентификатор платёжного канала.
     */
    String channel();
}
